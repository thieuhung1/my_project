// ============================================================
// ThemeProvider.jsx - Provider giao diện hiện tại của ứng dụng
// File này đang là placeholder cho theme, sẵn sàng mở rộng sau này.
// ============================================================

import React from 'react';

const ThemeProvider = ({ children }) => {
  return <>{children}</>;
};

export default ThemeProvider;
