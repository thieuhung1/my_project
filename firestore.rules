rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMP RELAXED FOR SEEDING - REMOVE AFTER!
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
